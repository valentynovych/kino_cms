name: Build & Deploy
on:
  push:
    branches:
      - master
      - main
jobs:
  build-deploy:
    name: Build and Deploy spring boot application
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'corretto'
          java-version: 17

      - name: Unit Tests
        run: mvn -B test --file pom.xml
        env:
          urlDB: localhost:3306/avada_slj_kcms03
          usernameDB: avada_slj_kcms03
          passwordDB: m8rqH2D1BC

      - name: Build application
        run: |
          mvn clean
          mvn -B package --file pom.xml